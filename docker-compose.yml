services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: xmldb
      POSTGRES_USER: xmldb
      POSTGRES_PASSWORD: xmldb
    ports:
      - "5432:5432"
    volumes:
      - ./src/main/resources/db/init:/docker-entrypoint-initdb.d:ro
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U xmldb -d xmldb"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: xmldb
      DB_USER: xmldb
      DB_PASSWORD: xmldb

volumes:
  pgdata:
